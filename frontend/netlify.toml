[build]
  base = "."
  command = "npm run build"
  publish = "build"
  functions = "netlify/functions"

[dev]
  targetPort = 3001
  port = 8888

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
